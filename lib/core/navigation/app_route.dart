enum AppRoute {
  // Initial
  splash,

  // Auth
  login,
  register,

  // Main tabs (shown in bottom nav)
  home,
  categories,
  cart,
  favorites,
  profile,

  // Product
  products,
  productDetail,

  // Checkout flow
  checkout,
  orderSuccess,

  // Profile sub-screens
  ordersHistory,
}

enum AppTab {
  home,
  categories,
  cart,
  favorites,
  profile,
}
